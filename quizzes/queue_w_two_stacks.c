#include <stdio.h>

struct queue_t
{
    stack_t stacks[2];
    bool is_ready_to_remove = 0;
}

Enqueue();
{
    if (is_ready_to_remove)
    {
        // move all elements to stack with index 0
    }
    // add element to stack with index 0;
}

Dequeue();
{
    if (!is_ready_to_remove)
    {
        // move all elements to stack with index 1
    }
    // remove element from stack with index 1;
}

int main(void)
{

    return 0;
}
